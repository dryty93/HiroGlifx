$ beg var $=b

$ inter var $=i

$ advan var $= a

$^ state var ^$=UI(Guess a number between 1 and 100:)

# ans var #=RAND100

$ ex var $=EXIT

write( $ beg var $)
!IF-NOT( $^ state var ^$ == # ans var # )!{
write(Wrong Answer)}

!if( $^ state var ^$ > # ans var # )!{
write(Your guess is too high)}

!if( $^ state var ^$ < # ans var # )!{
write(Your guess is too low)}

!if( $^ state var ^$ == # ans var # )!{
write(Correct), brk}




