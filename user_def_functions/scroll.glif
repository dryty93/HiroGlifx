/* HiroGlifx functions accept up to 5 parameter values.

# num var #= 1

def func test n()
    {
    @ds test var @ds=write(hi)
    }

def func subtract(x,y,n,i)
    {
    x*y+n*i*i-i**x-n+i-x**i
    # h var #= x
    @ds sub_holder var @ds= subtract_func(2,2,4,33)
    }
def func add(x,y)
    {
    x+y- 8
    @ds add_hold var @ds= add_func(590,4)
    }
write(# add_hold var #)


def func multiply(x,y)
    {
    x*y
    @ds mult_holder var @ds = multiply_func(2,2)
    }
write(# mult_holder var #)

def func divide(x,y)
    {
    x/y
    @ds div_hold var @ds= divide_func(8,2)
    }

!if(# sub_holder var # < # div_hold var #)!{
write(# sub_holder var #)
}
brk
